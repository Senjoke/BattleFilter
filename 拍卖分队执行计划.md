# 拍卖分队功能执行计划

## 0. 本次计划目标

新增一套独立的“拍卖分队”玩法：

- 管理员后台左侧菜单新增“拍卖分队”。
- 管理员可以抽取队长、抽取当前拍卖队员、确认成交。
- 队长可以通过报名大厅顶部“拍卖登陆”入口，使用管理员发放的临时拍卖码进入拍卖页面。
- 管理员和队长看到同一套拍卖局面：当前拍卖队员、未分配选手池、各队队长、队员、当前最高竞价、剩余资金。
- 队长通过加减按钮调整竞价并确认出价，前后端同时校验竞价必须高于当前价且不能超过本队剩余资金。
- 管理员可以处理成交、流拍、手动分队、重置拍卖会。
- 管理后台左侧菜单增加桌面端收缩/展开按钮。

本功能允许新增数据库结构，但应避免无关模块改动。

## 1. 需要先确认的规则

以下规则会影响数据库字段和接口行为，建议执行前确认：

1. 拍卖队伍人数：默认按 5 人一队计算建议队伍数，所以报名 20 人建议 4 队，21 人建议 5 队；管理员抽取队长前也可以手动输入队伍数，最少 2 队。
2. 队长抽取条件：系统综合总报名人数、建议队伍数、管理员手动队伍数、大段人数来判断某个大段是否可选；目标大段人数必须大于等于目标队伍数才允许抽取。
3. 初始竞拍资金：队员初始竞拍价为 0；每队初始剩余资金需要按队伍数量动态平衡。暂定以 4 队每队 90 为基准，总资金池为 `4 * 90 = 360`，每队初始资金为 `Math.round(360 / teamCount)`，执行前可按你的最终规则调整。
4. 起拍价与加价规则：默认起拍价 0；每次加减步进为 1；队长出价必须大于当前最高价且不能超过本队剩余资金；当本队剩余资金不足以达到 `当前最高价 + 1` 时，确认出价按钮置灰。
5. 出价输入方式：不使用手动文本输入，改为左减右加按钮调整出价，最低限制为 `当前最高价 + 1`，最高限制为本队剩余资金。
6. 下一位按钮状态：管理员抽出当前拍卖队员后，在没有“拍卖成交”或“流拍”之前，“下一位”按钮灰色不可点击。
7. 流拍规则：无人竞价时管理员可以点击“流拍”；流拍队员不从未分配选手池剔除，等待下次再次出现；系统需要记录该玩家是否流拍过以及累计流拍次数。
8. 匿名竞拍模式：当剩余可竞拍队员数量达到或低于 `队伍数量 + 2` 后开启匿名模式。队长端未分配选手池全部匿名，不显示战网 ID、职责和段位；当前竞拍区域隐藏战网 ID，但展示主要职责和主要职责段位。管理员端当前竞拍队员也默认隐藏战网 ID；管理员端未分配选手池使用 hover 遮挡，未 hover 时隐藏，hover 后可查看。
9. 手动分队规则：管理员可以将未分配选手池中的选手手动分到某个拍卖队伍，用于避免所有队伍资金不足导致选手反复流拍。手动分队应记录为手动分配，不扣或是否扣资金需要执行前确认，建议默认不扣资金。
10. 重置规则：管理员可以删除当前拍卖分队、队长、竞价和队员状态，回到初始状态，方便完成一轮竞拍后进入下一轮。
11. 队长段位筛选：按玩家三项职责段位中的“大段”统计。若一个玩家多个职责有不同大段，建议按该玩家最高大段归类，用于“目标段位人数”统计和队长抽取。
12. 拍卖结果归属：本计划默认拍卖结果只保存在拍卖分队表，不写入现有 `teams` / `matches`，避免影响 5V5、6V6 分队和赛程。若后续需要用拍卖结果发布赛程，可另加“发布为队伍”功能。
13. 临时拍卖码：建议每位队长一个随机码，仅当前拍卖会有效；管理员可在抽取队长后看到并私下发给队长。

## 2. 数据库计划

在 `database/schema` 目录新增：

- `00_ultimate_init_v3.sql`：基于现有 `00_ultimate_init_v2.sql` 的全量初始化脚本，追加拍卖相关表。
- `07_add_auction.sql`：给已有数据库执行的增量迁移脚本。

建议新增表：

### 2.1 `auction_sessions`

用于记录每个租户当前拍卖会。

核心字段：

- `id`
- `tenant_id`
- `status`：`setup` / `active` / `finished`
- `team_count`
- `initial_budget`
- `anonymous_mode`：是否已进入匿名竞拍模式
- `current_player_registration_id`
- `current_highest_team_id`
- `current_highest_bid`
- `last_action`：最近一次动作，可记录 `draw_player` / `sold` / `passed` / `manual_assigned`
- `created_at`
- `updated_at`

### 2.2 `auction_teams`

用于记录拍卖队伍、队长、临时拍卖码和资金。

核心字段：

- `id`
- `tenant_id`
- `session_id`
- `name`
- `captain_registration_id`
- `captain_code`
- `budget_total`
- `budget_remaining`
- `locked_bid_amount`：当前拍卖中该队已出的最高价，可选字段；也可只通过 `auction_bids` 计算
- `created_at`
- `updated_at`

说明：

- 队长本身不进入未分配选手池。
- 队长是否计入队伍成员展示：建议作为队伍固定身份展示在队伍卡片顶部，不放入被拍卖队员列表。

### 2.3 `auction_players`

用于记录报名玩家在当前拍卖会中的状态。

核心字段：

- `id`
- `tenant_id`
- `session_id`
- `registration_id`
- `status`：`pool` / `captain` / `current` / `sold` / `manual_assigned`
- `team_id`
- `sold_price`
- `pass_count`
- `last_passed_at`
- `assigned_type`：`auction` / `manual`
- `created_at`
- `updated_at`

说明：

- 抽为队长：`status = captain`。
- 当前被拍卖：`status = current`。
- 成交后：`status = sold`，写入 `team_id` 和 `sold_price`。
- 未分配池：`status = pool`。
- 流拍后：`status` 回到 `pool`，`pass_count + 1`，写入 `last_passed_at`。
- 手动分队后：`status = manual_assigned`，写入 `team_id` 和 `assigned_type = manual`。

### 2.4 `auction_bids`

用于记录出价历史，便于追溯和刷新页面恢复状态。

核心字段：

- `id`
- `tenant_id`
- `session_id`
- `team_id`
- `player_registration_id`
- `bid_amount`
- `created_at`

### 2.5 `auction_events`（可选）

用于记录成交、流拍、手动分队、重置等操作日志。如果实现时为减少表数量，也可以不建此表，只依赖 `auction_players`、`auction_bids` 和 `auction_sessions.last_action`。

核心字段：

- `id`
- `tenant_id`
- `session_id`
- `event_type`：`draw_captains` / `draw_player` / `bid` / `sold` / `passed` / `manual_assigned` / `reset`
- `registration_id`
- `team_id`
- `amount`
- `metadata`
- `created_at`

## 3. 后端接口计划

新增 `backend/src/routes/auction.ts`，并在 `backend/src/app.ts` 挂载：

- 管理员接口：`/api/admin/auction`
- 队长公开接口：`/api/auction`

### 3.1 管理员接口

1. `GET /api/admin/auction/state`
   - 获取当前拍卖会完整状态。
   - 返回队伍、队长、临时码、当前拍卖队员、最高竞价、未分配池、段位人数统计、建议队伍数、匿名模式状态、可执行动作状态。
   - 管理员端可以拿到完整数据，但匿名模式下前端默认遮挡当前竞拍战网 ID 和未分配池信息，未分配池 hover 后显示。

2. `POST /api/admin/auction/reset`
   - 删除当前租户拍卖分队、队长码、竞价、拍卖队员状态。
   - 回到初始状态，便于下一轮竞拍。
   - 该操作需要前端二次确认。

3. `GET /api/admin/auction/captain-rank-options`
   - 返回可抽取队长的大段统计。
   - 根据默认建议队伍数或管理员输入的 `teamCount` 返回可选状态。
   - 只允许选择人数满足 `teamCount` 的段位。

4. `POST /api/admin/auction/draw-captains`
   - 入参：`rankTier`、可选 `teamCount`。
   - 若未传 `teamCount`，按总报名人数自动计算建议队伍数。
   - 初始资金由后端按队伍数量计算，不从前端手动传入。
   - 从目标大段随机抽取队长。
   - 生成队伍和临时拍卖码。
   - 队长从未分配选手池移除。

5. `POST /api/admin/auction/draw-player`
   - 从未分配选手池随机抽取 1 名当前拍卖队员。
   - 若已有未成交的当前拍卖队员，则不允许再次抽取。
   - 若上一位没有成交或流拍，接口返回错误；前端对应“下一位”按钮置灰。
   - 抽取后当前最高竞价重置为 0。

6. `POST /api/admin/auction/finish-current`
   - 将当前拍卖队员成交给最高出价队伍。
   - 扣减队伍资金。
   - 当前拍卖队员从未分配池移入队伍名下。
   - 若无人出价，接口返回错误，不允许成交，管理员可选择“流拍”。

7. `POST /api/admin/auction/pass-current`
   - 当前无人竞价时管理员可点击“流拍”。
   - 将当前拍卖队员放回未分配选手池。
   - `pass_count + 1`，记录最近流拍时间。
   - 清空当前拍卖队员和当前最高竞价。

8. `POST /api/admin/auction/manual-assign`
   - 入参：`registrationId`、`teamId`。
   - 管理员从未分配选手池手动分配玩家到队伍。
   - 用于处理所有队伍资金不足导致无限流拍的情况。
   - 建议默认不扣资金，若需要扣资金应在执行前确认。

### 3.2 队长接口

1. `POST /api/auction/login`
   - 入参：`captainCode`。
   - 验证通过后返回一个队长访问 token 或临时身份信息。

2. `GET /api/auction/state`
   - 队长查看拍卖局面。
   - 不返回其他队长的临时拍卖码。
   - 匿名模式下不向队长端返回未分配池玩家的战网 ID、职责、段位。
   - 匿名模式下当前竞拍队员只返回主要职责和主要职责段位，不返回战网 ID。

3. `POST /api/auction/bid`
   - 入参：`bidAmount`。
   - 后端根据队长 token 找到对应队伍。
   - 校验：
     - 当前必须有正在拍卖的队员。
     - 出价必须大于当前最高价，最低为 `当前最高价 + 1`。
     - 出价不能超过本队剩余资金。
     - 出价步进为 1。
     - 当剩余资金不足以达到最低出价时，接口返回错误。
     - 已经拥有最高价的队伍重复出价时，应允许提高出价，但同样校验资金。

## 4. 后端实现细节

- 复用现有 `tenantMiddleware`，所有拍卖数据按 `tenant_id` 隔离。
- 管理员接口继续复用现有 `authMiddleware`。
- 新增队长访问校验中间件，避免队长访问管理员接口。
- 随机抽取使用数据库事务，避免重复抽取或成交时状态不一致。
- 队长抽取、下一位、成交、流拍、手动分队、重置都必须走后端状态机，前端只负责触发动作。
- 动态资金由后端统一计算并写入 `auction_teams.budget_total` / `budget_remaining`，避免前端伪造。
- 出价金额后端再次校验：必须为整数，必须大于当前最高价，必须不超过该队剩余资金。
- “下一位”接口必须校验当前拍卖队员是否已经成交或流拍，未处理前拒绝抽取。
- “流拍”接口只允许在当前队员无人出价时执行；流拍后玩家回到池子并累加流拍次数。
- 匿名模式由后端根据剩余可竞拍队员数量计算并返回 `anonymousMode`，队长接口在匿名模式下应直接脱敏敏感字段，不能只依赖前端隐藏。
- 所有写操作后清理拍卖状态缓存；如果拍卖状态不做 Redis 缓存，则先保持直接查库，减少复杂度。
- 段位大段识别复用现有段位文本规则，兼容：青铜、白银、黄金、白金、翡翠、钻石、大师、宗师、英杰、未定级。
- 未定级默认不参与队长目标段位选择，但仍保留在未分配选手池，可被拍卖。

## 5. 管理员前端计划

修改 `frontend/src/views/AdminView.vue`：

1. 左侧菜单新增：
   - `拍卖分队`
   - 建议使用 `Gavel`、`BadgeDollarSign` 或相近 lucide 图标。

2. 新增桌面端菜单收缩按钮：
   - 保留移动端现有展开逻辑。
   - 桌面端收缩后仅显示图标，菜单文字隐藏。
   - 当前选中状态、退出按钮、标题区域适配收缩态。

3. 新增拍卖分队页签内容：
   - 顶部控制区：
     - 显示总报名人数、建议队伍数、当前拍卖状态。
     - 提供目标队伍数输入框，默认填入建议队伍数，最少 2 队。
     - 显示大段人数统计。
     - 根据当前目标队伍数判断大段是否可选，只允许点击满足队长数量的大段。
     - “抽取队长”按钮。
     - “下一位”按钮：当前队员未成交或未流拍前置灰不可点。
     - “拍卖成交”按钮。
     - “流拍”按钮：无人竞价时可点，点击后该玩家回到未分配池并累计流拍次数。
     - “重置拍卖”按钮：删除当前分队、队长、竞价和拍卖状态，需要二次确认。
   - 当前拍卖队员区域：
     - 战网 ID。
     - 主要职责。
     - 主要职责段位。
     - 当前最高竞价和出价队伍。
     - 匿名模式下管理员端默认隐藏战网 ID，只展示主要职责和主要职责段位。
   - 未分配选手池：
     - 战网 ID。
     - 主要职责及主要职责段位。
     - 流拍次数。
     - 手动分队操作：选择目标队伍后可将玩家直接分配到该队。
     - 不展示已抽为队长、当前拍卖中、已成交玩家。
     - 匿名模式下管理员端未 hover 时遮挡信息，hover 后显示完整信息。
   - 队伍列表：
     - 队伍名称。
     - 队长战网 ID。
     - 临时拍卖码。
     - 剩余竞拍资金。
     - 已成交队员列表和成交价。
     - 手动分配队员需要标识为“手动”。

4. 页面样式：
   - 只参考用户提供图片的页面框架。
   - 不照搬图片颜色。
   - 使用当前系统灰色侧栏、白色内容卡片、蓝/绿/红操作色的既有风格。

## 6. 队长前端计划

新增或修改：

- `frontend/src/views/RegistrationView.vue`
  - 顶部导航增加“拍卖登陆”入口。

- `frontend/src/views/AuctionLoginView.vue`
  - 输入临时拍卖码。
  - 调用 `POST /api/auction/login`。
  - 登录成功进入 `/auction`。

- `frontend/src/views/AuctionView.vue`
  - 队长拍卖分队界面。
  - 与管理员拍卖页面复用相同状态展示结构。
  - 隐藏管理员专属操作：抽队长、下一位、成交、流拍、手动分队、重置。
  - 显示队长出价步进控件：左减右加，每次 1 点。
  - 最低出价为 `当前最高价 + 1`，最高出价为本队剩余资金。
  - 若本队剩余资金不足以出价，“确认出价”按钮置灰。
  - 出价按钮前端先校验最低价、最高价、步进，后端再做最终校验。
  - 匿名模式下未分配选手池完全匿名，不显示战网 ID、职责和段位。
  - 匿名模式下当前竞拍队员隐藏战网 ID，只展示主要职责和主要职责段位。

- `frontend/src/router/index.ts`
  - 新增 `/auction-login`。
  - 新增 `/auction`。

## 7. 共享展示与代码组织计划

为减少重复和后续维护风险，建议新增组件：

- `frontend/src/components/AuctionBoard.vue`

组件负责展示：

- 当前拍卖队员。
- 未分配选手池。
- 队伍卡片。
- 当前竞价。
- 匿名模式下的信息遮挡。
- 流拍次数和手动分配标识。
- 队长端步进出价控件。

管理员页面和队长页面分别传入不同操作插槽或 props：

- 管理员：显示抽取、成交、流拍、手动分队、重置。
- 队长：显示步进出价和确认出价。

如果为了最小改动，也可以先写在 `AdminView.vue` 和 `AuctionView.vue` 内部，但计划推荐组件化以降低重复。

## 8. 与现有功能的关系

- 不改动现有 5V5/6V6 分队数据结构。
- 不改动现有赛程发布逻辑。
- 不改动现有玩家看板双面板逻辑。
- 拍卖分队使用报名数据作为玩家来源，但拍卖结果独立保存。
- 已在 5V5/6V6 队伍中的玩家，仍可参与拍卖玩法；拍卖玩法和原分队玩法互不影响。
- 管理员清空报名数据时，后续需要同步考虑是否清空拍卖会。建议在拍卖接口里做状态保护，不在报名模块里额外改动。

## 9. 实施步骤

1. 新增数据库脚本：
   - `database/schema/00_ultimate_init_v3.sql`
   - `database/schema/07_add_auction.sql`

2. 新增后端拍卖路由：
   - `backend/src/routes/auction.ts`
   - 同步生成/维护对应 JS、d.ts 文件，保持当前项目写法一致。
   - `backend/src/app.ts` 挂载路由。

3. 新增拍卖状态组装逻辑：
   - 读取 registrations。
   - 计算队长段位统计。
   - 计算未分配池。
   - 组装队伍、队员、当前拍卖、最高价。
   - 计算建议队伍数、动态初始资金、匿名模式状态。
   - 返回按钮可用状态：可抽队长、可抽下一位、可成交、可流拍、可出价。
   - 返回流拍次数、手动分配标识。

4. 新增管理员拍卖页面：
   - `AdminView.vue` 左侧菜单。
   - 拍卖分队页签。
   - 后台操作按钮和状态刷新。
   - 队伍数输入、大段可选状态、抽队长。
   - 下一位按钮状态控制。
   - 成交、流拍、手动分队、重置。
   - 匿名模式遮挡和 hover 显示。

5. 新增队长登录与拍卖页面：
   - 报名大厅顶部“拍卖登陆”。
   - `/auction-login`。
   - `/auction`。
   - 队长端步进出价控件。
   - 匿名模式下队长端数据脱敏展示。

6. 新增侧栏收缩展开：
   - 只改 `AdminView.vue` 布局相关状态和 class。
   - 保持移动端菜单现有行为。

7. 检查范围：
   - 确认未改动无关模块。
   - 确认数据库脚本命名正确。
   - 确认 TypeScript 类型不出现 `string | string[] | undefined` 类似构建问题。
   - 确认成交、流拍、下一位置灰、资金上限、匿名模式、手动分队都有前后端约束。
   - 如用户允许，再运行构建或本地验证。

## 10. 风险与规避

1. 竞价并发风险
   - 使用事务锁定当前拍卖 session 或当前 player 状态，避免两个队长同时出价导致最高价错乱。

2. 拍卖状态不一致
   - 抽取、成交、流拍、手动分队都通过后端状态机控制，不让前端直接改状态。
   - “跳过”不作为正式功能，改用“流拍”；成交或流拍前禁止抽下一位。

3. 队长码泄露
   - 队长码仅当前拍卖会有效，重置拍卖会后失效。
   - 队长接口不返回其他队伍的拍卖码。

4. 与现有分队玩法互相影响
   - 拍卖使用独立表，不写入 `teams` / `matches`。

5. 数据库初始化与增量脚本不一致
   - `00_ultimate_init_v3.sql` 和 `07_add_auction.sql` 同步维护同一套表结构。

6. 无限流拍风险
   - 提供管理员手动分队能力。
   - 流拍次数在未分配池展示，管理员可以识别反复流拍玩家。

7. 匿名模式泄露风险
   - 队长端由后端脱敏，不只靠前端 CSS 隐藏。
   - 管理员端保留完整数据，但界面默认遮挡，hover 后显示。

8. 资金规则边界风险
   - 前端按钮置灰只是体验优化，后端仍必须校验最低出价和最高资金。

## 11. 本轮不执行的内容

- 不直接修改业务代码。
- 不运行数据库迁移。
- 不构建前端或后端。
- 不改变现有数据库结构。
- 不提交或推送 GitHub。
