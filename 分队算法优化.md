# 守望先锋赛事分队算法优化说明

## 1. 摘要 (Summary)
本项目已经完成了 BattleFilter 的分队管理系统优化，重点改进了**自动分队**与**手动分队**算法。
自动分队引入了“基于群号隔离”以及“基于段位分数均衡（蛇形分组）”的逻辑，确保了对战组内的玩家来源一致且实力相近；同时保留了原有的 1-2-2 职责强制槽位，不足时允许空缺。
手动分队解除了职责与群号的硬性绑定，仅保留了队伍 5 人上限，并增加了“队伍内手动调整玩家职责”的功能，提供了极大的灵活性。

## 2. 优化对比 (Before vs After)

### 2.1 自动分队
- **优化前**：采取纯贪心算法凑齐 1重装 2输出 2支援 的职责。缺乏群号 (`wechat_group`) 过滤，不同群的玩家会被混编。分数完全硬编码为 0，没有实现“实力相近”。
- **优化后**：
  - **群号隔离**：管理员在前端点击“自动填充本组”时，会弹出选择框选择“微信群号”，算法只会将该群的玩家自动分配进本对战组。
  - **段位分数转换**：增加了将自填段位（如"黄金1", "白银5"）转换为数值分数的算法（青铜=1000，白银=1500，依此类推，每个小段位±100分）。
  - **蛇形实力均衡**：采用**职责法**优先获取玩家在被分配位置的段位分。按照各职责进行实力倒序排序后，采用蛇形分组（Snake Draft）交替分配给 A、B 队伍，同时在每次分配时动态对比两队当前总分，自动补齐弱势队伍，最大程度保证两队分差最小。

### 2.2 手动分队
- **优化前**：前端严格校验玩家的意向职责与队伍空缺槽位，如果不匹配直接拦截；且队伍成型后无法修改玩家的具体职责定位。
- **优化后**：
  - **无视职责越权加入**：完全移除了 `assignedRole` 的严格空缺检测和群号检测，只保留队伍 `members.length < 5` 的上限拦截。可以强制把一个只会玩“重装”的玩家塞进缺“输出”的队伍。
  - **队伍内职责动态调整**：为每个队伍成员增加了“职责编辑下拉框”，管理员可以随时将队伍内的玩家职责手动调整（例如将输出调整为补位或支援），并且实时同步持久化到数据库。

## 3. 核心算法细节

### 3.1 分数转换公式
```typescript
const rankToScore = (rankStr: string | undefined): number => {
  if (!rankStr || rankStr === '未定级') return 1500; // 默认白银
  const tiers = { '青铜': 1000, '白银': 1500, '黄金': 2000, '白金': 2500, '钻石': 3000, '大师': 3500, '宗师': 4000, '英杰': 4500 };
  // ...正则提取...
  // 5是最低，1是最高，所以 (5 - sub) * 100
  return baseScore + (5 - sub) * 100;
};
```
获取玩家分数优先判断其“担任职责”的段位，若该职责未定级，则退化取其所有定级位置的**平均分**。

### 3.2 动态蛇形分配策略
在自动分配某职责（如输出）时：
1. 计算队伍 A 和 B 目前一共缺几名输出。
2. 挑出前 N 名实力最高的玩家。
3. 循环分配：
   - 比较当前 Team A 和 Team B 的已有总分（`scoreA` vs `scoreB`）。
   - 如果 A 队总分低于 B 队，并且 A 队缺输出，优先把该强力玩家塞给 A 队。
   - 反之塞给 B 队。
   - 这种基于当前实时总分的贪心补弱策略，比死板的“1-2-2-1”蛇形顺序更加均衡。

## 4. UI交互体验提升
- 增加了 **目标微信群下拉弹窗** 保证自动组队的来源精准可控。
- 在队伍名称旁边，动态渲染了 **队伍平均实力分**，直观显示队伍的战斗力。
- 移除了强制槽位的强绑定，空缺位置明确显示为“空缺”或“补位/溢出”，更符合灵活赛事的直觉。