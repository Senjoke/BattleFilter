# BattleFilter 守望先锋赛事自动组队系统 - 部署与运行文档

本文档详细说明了如何将本项目的 **数据库 (PostgreSQL)**、**后端 (Node.js/Express)** 和 **前端 (Vue 3/Vite)** 部署到服务器上并进行相关配置。

---

## 💻 1. 服务器环境准备

在开始部署之前，请确保您的服务器（如 CentOS, Ubuntu, Debian 等）已安装以下基础软件：
- **Node.js**: 推荐版本 `v18.x` 或以上。
- **npm / pnpm / yarn**: Node.js 附带的包管理工具。
- **Docker & Docker Compose**: 用于快速、稳定地部署数据库。
- **PM2**: Node.js 的生产环境进程管理工具（使用 `npm install -g pm2` 安装）。
- **Nginx**: 用于部署前端静态文件以及反向代理后端 API。

---

## 🗄️ 2. 数据库部署 (PostgreSQL)

我们强烈推荐使用 Docker 容器化部署 PostgreSQL 数据库，这能避免繁琐的环境依赖问题。

### 2.1 配置文件与启动
在项目根目录下有一个 `docker-compose.yml` 文件。如果没有，可以手动创建一个：

```yaml
version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: battlefilter-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: battlefilter
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
```

**启动数据库：**
在 `docker-compose.yml` 所在的目录，运行以下命令：
```bash
docker-compose up -d
```

### 2.2 推荐的默认配置
- **主机 (Host)**: `localhost` (若后端与数据库在同一台服务器)
- **端口 (Port)**: `5432`
- **用户名 (User)**: `admin`
- **密码 (Password)**: `password` (线上生产环境请务必修改为强密码)
- **数据库名 (DB Name)**: `battlefilter`

> **注意**：数据库启动后，后端服务首次连接时会自动执行建表脚本，您不需要手动建表。

---

## ⚙️ 3. 后端部署 (Node.js/Express)

后端负责提供所有的数据接口（API）以及数据库操作逻辑。

### 3.1 环境变量配置
进入 `backend` 目录：
```bash
cd backend
```
在 `backend` 目录下创建或修改 `.env` 文件，填入您的实际配置：

```env
# 服务器运行端口
PORT=3000

# 数据库连接配置
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=password
DB_NAME=battlefilter

# JWT 密钥 (请在生产环境中修改为长随机字符串)
JWT_SECRET=your_super_secret_jwt_key
```

### 3.2 安装依赖与编译
```bash
# 1. 安装后端依赖包
npm install

# 2. 编译 TypeScript 代码到 JavaScript (输出到 dist 目录)
npm run build
```

### 3.3 生产环境启动 (使用 PM2)
使用 PM2 启动后端，可以保证在服务器重启或程序崩溃时自动重启：
```bash
# 启动后端服务
pm2 start dist/index.js --name "battlefilter-backend"

# 保存 PM2 当前的进程列表，以便开机自启
pm2 save
pm2 startup
```

### 3.4 🛠️ 如何修改后端 API？
如果您需要新增或修改业务逻辑：
1. API 路由文件存放在 `backend/src/routes/` 目录下（如 `teams.ts`, `registration.ts`, `matches.ts`）。
2. 修改完成后，需要重新编译 TypeScript 并重启 PM2 服务：
   ```bash
   cd backend
   npm run build
   pm2 restart battlefilter-backend
   ```

---

## 🌐 4. 前端部署 (Vue 3 + Vite)

前端是一个纯静态的单页应用（SPA），我们将代码打包后使用 Nginx 进行部署。

### 4.1 环境变量配置
进入 `frontend` 目录：
```bash
cd frontend
```
在 `frontend` 目录下找到或创建 `.env` 文件（生产环境推荐使用 `.env.production`），配置请求后端的 API 根地址：

```env
# 生产环境 API 基础路径
# 请修改为您服务器的真实域名或 IP。例如：http://123.45.67.89:3000/api 或 https://api.yourdomain.com/api
VITE_API_BASE_URL=/api
```
*(如果通过同一台服务器的 Nginx 反向代理，直接写 `/api` 即可，详见下文 Nginx 配置)*

### 4.2 安装依赖与打包
```bash
# 1. 安装前端依赖
npm install

# 2. 打包构建前端静态文件
npm run build
```
打包完成后，`frontend` 目录下会生成一个 `dist` 文件夹，这就是最终要部署的静态资源。

### 4.3 使用 Nginx 部署
在您的服务器上配置 Nginx。编辑 Nginx 配置文件（如 `/etc/nginx/conf.d/battlefilter.conf`）：

```nginx
server {
    listen 80;
    server_name your_domain_or_ip; # 修改为您的域名或服务器 IP

    # 1. 部署前端静态页面
    location / {
        root /path/to/your/BattleFilter/frontend/dist; # 替换为前端 dist 文件夹的绝对路径
        index index.html index.htm;
        # 这一句非常重要，解决 Vue Router 刷新后 404 的问题
        try_files $uri $uri/ /index.html;
    }

    # 2. 反向代理后端 API
    location /api/ {
        # 代理到本地的 Node.js 后端端口
        proxy_pass http://127.0.0.1:3000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

配置完成后，重启 Nginx 使其生效：
```bash
# 检查配置语法是否正确
nginx -t

# 重启 Nginx
systemctl restart nginx
```

---

## 🎯 5. 常见问题排查 (FAQ)

1. **前端页面访问出现 404 错误**
   - 检查 Nginx 的 `try_files $uri $uri/ /index.html;` 是否配置正确。
2. **前端发请求提示 `Network Error` 或 `CORS` 跨域报错**
   - 检查前端打包时的 `VITE_API_BASE_URL` 是否指向了正确的后端地址。
   - 推荐使用上述 Nginx 的 `location /api/` 反向代理方案，彻底避免跨域问题。
3. **后端连接数据库失败 `Connection Refused`**
   - 检查 `docker ps` 中 PostgreSQL 容器是否正常运行。
   - 检查 `backend/.env` 中的密码、端口与 `docker-compose.yml` 中是否完全一致。
4. **如何查看后端运行日志？**
   - 运行命令：`pm2 logs battlefilter-backend`。

---
> 🚀 **至此，整个 BattleFilter 赛事系统已在您的服务器上完整运行！您可以在浏览器中输入服务器 IP 访问报名大厅了！**
