# 守望先锋赛事分队管理与算法说明

本文档主要阐述当前系统（BattleFilter）在分队管理模块中使用的**自动分队**和**手动分队**相关的规则与核心算法，特别针对**职责限制**（位置锁定）进行了详细说明。

## 1. 核心职责限制（5v5 槽位机制）

为了保证对局的公平性和符合《守望先锋》常规 5v5 职责锁定模式，系统对每个队伍强制施加了**固定职责槽位限制**。

*   **队伍容量**：每支队伍固定 5 人。
*   **槽位分配**：
    *   **重装（Tank）**：1 名
    *   **输出（Damage）**：2 名
    *   **支援（Support）**：2 名
*   **限制原则**：无论是自动分队还是手动分队，任何队伍的成员构成必须严格按照上述槽位进行“对号入座”。如果某一职责的槽位已满，系统将从底层算法拦截并拒绝将更多拥有该职责意向的玩家分配到该队伍。

## 2. 自动分队算法

自动分队是以**“对战组（Team Group）”**为单位进行。每个对战组固定包含两支队伍（例如队伍 A 和队伍 B）。

### 2.1 数据准备
1.  **确定未分配选手池**：获取当前周期（本周）所有已报名且**尚未被分配**到任何有效对战组的玩家，作为待选池。
2.  **职责合并与去重**：对于未分配的玩家，将其报名时填写的“首选职责（primary_roles）”和“次选职责（secondary_roles）”进行合并与去重，形成该玩家可胜任的“综合职责列表（roles）”。

### 2.2 自动填充逻辑
自动填充基于固定职责槽位要求（1重装、2输出、2支援）依次按顺序进行：

1.  **队伍遍历**：遍历当前对战组内的每一支队伍。
2.  **盘点空缺**：针对该队伍，统计当前已经存在的成员及其被分配的职责（`assignedRole`），计算各职责分别还需要补充多少人。
3.  **职责贪心匹配**：系统按照预设的职责顺序（重装 -> 输出 -> 支援）进行空位填补：
    *   当发现某个职责有空缺时，系统会在**未分配选手池**中进行线性搜索。
    *   寻找第一个其“综合职责列表”中包含该空缺职责的玩家。
    *   一旦找到匹配的玩家，立即将其从选手池中移除，加入到当前队伍中，并**强制锁定**其在该队伍中的“分配职责（`assignedRole`）”为当前补充的职责。
4.  **循环终止**：不断重复填补过程，直到队伍的三个职责槽位全部满编，或者选手池中再也找不到符合剩余空位职责要求的玩家为止。

*注：当前的自动分队采用的是以职责合规性为绝对优先级的“贪心算法（Greedy Algorithm）”，主要目标是快速生成一支符合 1-2-2 阵容合规性的队伍。*

## 3. 手动分队算法（人员微调与分配）

手动分队赋予了管理员针对未分配玩家进行单独或批量加入队伍的能力，同时也支持从队伍中踢出玩家退回未分配池。手动分配操作同样受到前端逻辑严格的职责限制保护。

### 3.1 单个/批量加入队伍检测算法（`tryAddPlayerToTeam`）
当管理员尝试将一名玩家加入指定队伍时，系统会执行一套拦截与匹配算法：

1.  **槽位快照提取**：系统首先生成该队伍的 5 个标准槽位快照，并将已有成员根据其分配的职责（`assignedRole`）分别落座。
2.  **提取空位列表**：计算队伍当前还剩下哪些空闲槽位（例如：还缺 1个输出、1个支援）。
3.  **玩家能力比对**：读取该玩家在报名时填写的原始职责列表（包含所有可玩的职责）。
4.  **寻找首个交集职责**：
    *   系统将队伍的“空缺职责”与玩家的“可玩职责”取交集。
    *   系统会寻找**第一个**匹配的职责作为结果。
5.  **分配与拦截**：
    *   **匹配成功**：允许玩家加入队伍，并在加入的瞬间将其在该队伍中的角色标记为这个匹配成功的职责。
    *   **匹配失败（拦截）**：如果队伍已满 5 人，或者玩家会玩的所有职责在队伍中都已满编（例如玩家只会玩重装，但队伍已经有重装了），系统将返回 `false`，操作被拦截并提示“队伍已经没有适合该玩家职责的空位了”。

### 3.2 批量加入的回退与汇总机制
在管理员勾选多名玩家执行“批量加入”操作时，系统会依次遍历这些玩家：
*   对每位玩家独立执行上述的检测算法。
*   符合条件的玩家会被成功塞入队伍空槽位；
*   遇到位置冲突的玩家，操作不会中断报错，而是将其跳过。在整个批量操作循环结束后，系统会弹窗汇总成功加入了多少人，并明确列出“因位置冲突或队伍已满未能加入”的选手名单。
