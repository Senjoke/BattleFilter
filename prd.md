# 守望先锋赛事自动组队系统 - 产品需求文档 (PRD)

## 1. 项目概述
本项目旨在为《守望先锋》社区赛事提供一个轻量级的自动化报名、战绩查询、队伍平衡与赛程安排系统。系统分为面向普通玩家的C端报名页面，以及面向赛事管理员的B端管理后台。核心特点是基于玩家真实战绩进行算法分队，且数据按周循环清理，不增加长期存储负担。

## 2. 技术栈架构
*   **前端框架**：Vue.js (配合 TailwindCSS 或响应式UI组件库如 Vant / Element Plus，严格要求 Web 端和移动端的自适应响应式布局)
*   **后端框架**：Node.js + Express (轻量级，适合快速开发接口)
*   **数据库**：PostgreSQL (关系型数据库，便于处理玩家、队伍、赛程等实体关联，同时支持高效的批量清空操作)
*   **第三方接口**：OWAPI v2 (核心依赖，用于拉取战网ID对应的游戏战绩和英雄数据)

## 3. 核心功能需求

### 3.1 C端：玩家报名页 (响应式 Mobile & Web)
*   **信息录入**：输入完整的战网ID (例如: Player#12345)。
*   **职责选择**：选择本次比赛希望担任的位置 (重装 Tank、输出 Damage、支援 Support，支持多选或主次偏好设置)。
*   **确认与提交**：展示本周比赛日期与基础规则，玩家确认后提交报名。
*   **状态反馈**：报名成功后的提示页面，以及后续的赛程/队伍信息公示看板。

### 3.2 B端：管理员控制台 (Web 端为主)
*   **安全认证**：管理员专属的登录验证机制。
*   **报名管理大厅**：
    *   列表展示本周所有已报名的战网ID及其职责偏好。
    *   支持手动增删改查报名记录。
*   **战绩获取与分析**：
    *   一键/自动调用 OWAPI v2，批量获取报名玩家的隐藏分/段位、各职责胜率及常用英雄数据。
    *   数据可视化展示玩家综合实力评分。
*   **算法智能分队**：
    *   基于玩家获取到的战绩评分和职责偏好，执行平衡算法。
    *   一键生成实力相近、职责合规（如每队1T2C2S）的队伍。
    *   支持管理员手动微调队伍人员名单。
*   **赛程排期安排**：
    *   根据生成的队伍，自动或手动排布对阵表（如单败淘汰、双败或瑞士轮）。
    *   生成可视化赛程图，并可发布至C端展示。
*   **周期重置管理**：
    *   “结束本周赛事”功能：一键清空/归档本周所有报名、战绩快照、队伍与赛程数据，为下周比赛腾出空间。

## 4. 数据库与数据流设计简述
由于数据以“周”为生命周期，不需要长期大量累积：
*   **`registrations` 表**：记录战网ID、报名职责、联系方式（可选）、报名时间。
*   **`player_stats` 表**：缓存 OWAPI 返回的战绩快照，防止频繁请求第三方 API 触发限流 (Rate Limit)。
*   **`teams` & `matches` 表**：存储生成的队伍结构和赛程对局。
*   **清理机制**：后端提供专属的重置接口，执行 `TRUNCATE` 或批量删除操作，快速初始化数据库状态。

## 5. 部署与上线规划
*   前后端分离部署。
*   前端编译为静态资源后可托管至 Nginx 或 Vercel 等平台。
*   后端 Node 服务与 PostgreSQL 数据库部署至云服务器（如阿里云、腾讯云等），使用 PM2 进行进程守护。

## 6. 安全与权限设计

### 6.1 C端：匿名报名防滥用机制
由于C端采用免注册（匿名）输入战网ID的形式进行报名，为防止恶意刷接口、提交虚假信息或占用名额，系统需引入以下防滥用机制：
*   **战网ID格式严格校验**：前端和后端同时对提交的战网ID进行正则校验（标准格式如 `Name#12345`）。
*   **账号真实性验证 (结合OWAPI)**：在提交报名时，后端尝试调用 OWAPI v2 检查该战网ID是否存在且战绩公开。若查无此人或生涯未公开，则拒绝报名，并提示用户公开生涯（保证后续能正确抓取数据进行分队）。
*   **重复报名限制**：数据库层针对本周赛事周期，对 `battle_tag` 字段设置唯一索引（Unique Constraint）或业务逻辑查重，防止同一玩家重复提交。
*   **IP 频次限制 (Rate Limiting)**：针对单一IP地址，限制其在一定时间内的报名提交次数（例如：同一IP每小时最多提交3次），防止脚本恶意批量塞入假账号。
*   **人机验证 (CAPTCHA)**：在报名表单提交前加入简单的图形验证码、Cloudflare Turnstile 或 reCAPTCHA，有效拦截自动化机器人的攻击。

### 6.2 B端：管理员权限与登录验证
管理员后台涉及“战绩批量拉取（可能触发API限流）”、“队伍结果修改”以及“一键清空本周数据”等高危核心操作，因此**必须**引入身份验证模块：
*   **独立登录页面**：B端入口需强制跳转登录页，未登录状态下不可访问任何管理看板和接口。
*   **JWT 身份鉴权 (JSON Web Token)**：后端验证账号密码后下发 JWT Token，B端前端将其存储，并在后续请求 API 时携带在 `Authorization` 请求头中。
*   **账号管理方案**：
    *   *初期方案*：鉴于系统轻量化，可通过后端环境变量 (`.env`) 硬编码一个管理员账号和密码，实现快速安全上线。
    *   *进阶方案*：在 PostgreSQL 中引入 `admins` 表，支持多管理员协同管理。
*   **后端接口拦截 (Middleware)**：所有B端专属 API 路由（如 `/api/admin/*`）必须挂载鉴权中间件。对于无效、过期或未携带 Token 的请求，一律拦截并返回 `401 Unauthorized`，确保系统底层的绝对安全。
