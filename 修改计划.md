# BattleFilter 5V5/6V6 分队与段位规则修改计划

## 一、项目现状理解

BattleFilter 当前由三部分组成：

- 前端：`frontend`，Vue 3 + Vite，主要页面为报名页、管理后台、赛事看板。
- 后端：`backend`，Express + TypeScript，核心 API 在 `backend/src/routes`，看板查询封装在 `backend/src/services/board.service.ts`。
- 数据库：PostgreSQL，初始化/迁移 SQL 在 `database/schema`。

当前与本次需求直接相关的现状：

- 报名信息统一存储在 `registrations` 表，包含战网 ID、微信、群组、首选/补位职责、自填段位。
- 分队结果存储在 `teams.members` JSONB 中，目前只有一套分队数据。
- 赛程存储在 `matches` 表，通过 `team1_id/team2_id` 关联 `teams`。
- 管理后台 `frontend/src/views/AdminView.vue` 当前已有“分队管理 (5v5)”页面逻辑，包括添加对战组、自动填充、手动加入、未分配选手池、队内职责调整、赛程发布/比分录入。
- 玩家看板 `frontend/src/views/BoardView.vue` 当前只展示一套队伍和赛程。
- 段位分数转换主要在 `backend/src/routes/teams.ts` 的 `rankToScore` 中使用，当前源码缺少“翡翠”，且分值范围与新需求不一致。

注意：当前工作区已有未提交改动，主要在 `backend/src/routes/teams.js` 及其 map/d.ts.map 编译产物中；其中 `teams.js` 已有部分 6v6/翡翠尝试，但 `teams.ts` 源码尚未同步。后续实现应以 TypeScript 源码为准，再通过构建同步产物，避免源码和运行产物漂移。

## 二、需求拆解

### 1. 段位大段分数规则

目标规则：

| 大段 | 分数范围 |
| --- | --- |
| 青铜 | `< 999` |
| 白银 | `1000 - 1499` |
| 黄金 | `1500 - 1999` |
| 白金 | `2000 - 2499` |
| 翡翠 | `2500 - 2999` |
| 钻石 | `3000 - 3499` |
| 大师 | `3500 - 3999` |
| 宗师 | `4000 - 4499` |
| 英杰 | `4500 - 5000` |

计划采用的细分段位换算：

- 继续兼容现有报名下拉中的 `大段 + 5/4/3/2/1` 格式。
- 每个大段内按 100 分递增：例如 `黄金5 = 1500`、`黄金4 = 1600`、`黄金3 = 1700`、`黄金2 = 1800`、`黄金1 = 1900`。
- `青铜5 - 青铜1` 映射为 `500 - 900`，确保属于 `<999`。
- `英杰5 - 英杰1` 映射为 `4500 - 4900`，保留 `5000` 作为上限边界。
- 未填/未定级继续按默认值处理，建议保持现状默认 `1500`，即黄金下限/中低段位基准；如需要更保守，也可改成 `1000`。

涉及文件：

- `backend/src/routes/teams.ts`
- `frontend/src/views/RegistrationView.vue`
- 视构建结果同步相关 `.js/.d.ts/.map` 产物

### 2. 新增 6V6 分队管理

新增菜单：

- 将现有菜单“分队管理”明确改名为“分队管理（5V5）”。
- 新增菜单“分队管理（6V6）”。

两套分队要求：

- 5V5：`1 坦克 + 2 输出 + 2 支援`。
- 6V6：`2 坦克 + 2 输出 + 2 支援`。
- 两套模式共用同一份报名信息。
- 两套模式的分队结果互相隔离：
  - 玩家 A 加入 5V5 队伍后，在 5V5 未分配池中消失。
  - 玩家 A 仍应在 6V6 未分配池中显示，可独立加入 6V6 队伍。
  - 反向同理。

前端计划：

- 抽象分队模式配置：
  - `5v5`：标题、菜单名、队伍人数上限 `5`、槽位 `1/2/2`、API 参数。
  - `6v6`：标题、菜单名、队伍人数上限 `6`、槽位 `2/2/2`、API 参数。
- 复用现有业务能力：
  - 添加对战组
  - 删除对战组
  - 自动填充/自动分队
  - 手动单人加入
  - 批量加入
  - 未分配选手池筛选/搜索
  - 队内移除选手
  - 队名编辑
  - 队内职责调整
  - 队伍均分展示
- `getTeamSlots` 改为按当前模式动态生成 5 个或 6 个职责槽位。
- `tryAddPlayerToTeam` 改为按当前模式的人数上限判断，5V5 上限 5，6V6 上限 6。
- 未分配选手池按当前模式的队伍成员独立计算。

涉及文件：

- `frontend/src/views/AdminView.vue`

### 3. 后端 API 与数据隔离

最终采用不改数据库结构的方案：

- 使用现有 `teams.group_id` 区分模式。
- `group-6v6-` 前缀归入 6V6。
- `group-5v5-` 前缀和历史 `group-时间戳` 归入 5V5。
- `matches` 不新增字段，通过关联双方队伍的 `group_id` 推断模式。

API 调整计划：

- `POST /api/admin/teams/group`
  - body 新增 `mode`，默认 `5v5`。
  - 创建队伍时写入带模式前缀的 `group_id`。
- `DELETE /api/admin/teams/group/:groupId`
  - 按 `tenant_id + group_id` 删除对应队伍与关联赛程。
- `POST /api/admin/teams/group/:groupId/autofill`
  - 从目标 group 的 `group_id` 前缀获取模式。
  - 按模式使用不同职责槽位：5V5 为 `1/2/2`，6V6 为 `2/2/2`。
  - 统计已分配玩家时只统计同一 mode 下的 teams。
- `POST /api/admin/teams/edit`
  - 继续按 `teamId` 更新成员/队名。
  - 清理对应 mode 的看板缓存。
- `GET /api/board/teams?mode=5v5|6v6`
  - 新增 `mode` 查询参数，默认 `5v5`。
  - 返回指定模式的队伍。
- `POST /api/admin/matches/generate`
  - body/query 新增 `mode`，默认 `5v5`。
  - 只为指定 mode 的队伍生成赛程。
  - 只清空指定 mode 的旧赛程，不影响另一个模式。
- `DELETE /api/admin/matches/clear`
  - 支持按 mode 清空。
- `GET /api/board/matches?mode=5v5|6v6`
  - 新增 `mode` 查询参数，默认 `5v5`。
  - 返回指定模式的赛程。
- `POST /api/admin/matches/update` 与 `/score`
  - 继续按 matchId 更新，但缓存清理需要能命中对应 mode。

涉及文件：

- `backend/src/routes/teams.ts`
- `backend/src/routes/matches.ts`
- `backend/src/services/board.service.ts`
- `backend/src/controllers/board.controller.ts`

### 4. 赛程发布同步

管理后台赛程目前只有一套 `matches` 状态。计划改为按模式发布：

- 方案 A：在“赛程安排”页面增加模式切换：`5V5 / 6V6`。
- 方案 B：拆成两个菜单：`赛程安排（5V5）` 与 `赛程安排（6V6）`。

建议采用方案 A，避免左侧菜单过长，也与玩家看板的模式切换保持一致。

行为：

- 选中 5V5 时，发布/清空/保存/录分只操作 5V5 赛程。
- 选中 6V6 时，发布/清空/保存/录分只操作 6V6 赛程。
- 赛程生成时只使用对应模式下的队伍。

涉及文件：

- `frontend/src/views/AdminView.vue`
- `backend/src/routes/matches.ts`

### 5. 玩家赛事看板同步

玩家看板需要能查看 5V5 与 6V6 两套赛事面板。

计划：

- 在 `BoardView.vue` 增加 `5V5 / 6V6` 模式切换。
- 队伍阵容、赛程安排按当前模式分别请求：
  - `/board/teams?mode=5v5`
  - `/board/matches?mode=5v5`
  - `/board/teams?mode=6v6`
  - `/board/matches?mode=6v6`
- 已报名选手继续共用 `/board/registrations`，不按模式区分。
- 队伍卡片中职责展示优先使用 `assignedRole`，修正当前看板使用 `m.role` 导致可能显示为空的问题。
- 对战组筛选也按当前模式独立计算。

涉及文件：

- `frontend/src/views/BoardView.vue`
- `backend/src/services/board.service.ts`

## 三、最终执行约束与实施顺序

根据后续确认，本次实现不修改现有数据库结构，不新增迁移，不改任何 `database/schema` 文件。5V5/6V6 隔离改为使用现有 `teams.group_id` 约定：

- 新建 5V5 对战组使用 `group-5v5-时间戳`。
- 新建 6V6 对战组使用 `group-6v6-时间戳`。
- 历史旧格式 `group-时间戳` 默认归入 5V5。
- 赛程不新增字段，通过关联队伍的 `group_id` 推断所属模式。

原计划中的 `teams.mode`、`matches.mode`、新增迁移等数据库结构变更方案不再执行。

1. 数据库层
   - 不修改数据库结构。
   - 不新增迁移。
   - 不修改全量初始化 SQL。

2. 后端分队 API
   - 补齐统一段位分数转换，加入“翡翠”。
   - 创建/查询/自动分队按 mode 隔离。
   - 自动分队按 5V5/6V6 使用不同职责槽位。
   - 缓存 key 增加 mode 维度。

3. 后端赛程 API
   - 生成/清空/查询按 mode 隔离。
   - 更新/录分后清理对应 mode 缓存。

4. 管理后台
   - 左侧菜单改为“分队管理（5V5）”，新增“分队管理（6V6）”。
   - 将现有分队页面逻辑参数化，复用为两套模式。
   - 赛程安排增加模式切换，并按 mode 调用 API。

5. 玩家看板
   - 增加模式切换。
   - 分别展示对应模式的队伍与赛程。
   - 已报名选手保持共用。

6. 构建产物同步
   - 执行后端 `npm run build`，同步 `.js/.d.ts/.map` 产物。
   - 执行前端 `npm run build` 做类型和打包检查。

## 四、验证计划

### 后端复核

- 5V5 创建对战组后，`group_id` 前缀为 `group-5v5-`。
- 6V6 创建对战组后，`group_id` 前缀为 `group-6v6-`。
- 玩家 A 加入 5V5 后：
  - 5V5 未分配池不显示 A。
  - 6V6 未分配池仍显示 A。
- 玩家 A 加入 6V6 后：
  - 两套分队各自保存 A，不互相覆盖。
- 5V5 自动填充每队最多 5 人，职责目标为 `1/2/2`。
- 6V6 自动填充每队最多 6 人，职责目标为 `2/2/2`。
- 5V5 发布赛程不会删除/覆盖 6V6 赛程。
- 6V6 发布赛程不会删除/覆盖 5V5 赛程。

### 前端验证

- 管理后台左侧出现：
  - 报名大厅
  - 分队管理（5V5）
  - 分队管理（6V6）
  - 赛程安排
  - 赛事公告
  - 打赏管理
- 两个分队页面功能一致，但槽位数量不同。
- 6V6 队伍显示两个坦克槽位。
- 手动加入、批量加入、移除、职责调整后，未分配池实时正确。
- 玩家看板可切换 5V5/6V6，并展示对应队伍与赛程。

### 代码复核

- 按用户要求，不执行前端运行验证。
- 按用户要求，不执行后端运行验证。
- 通过静态阅读、差异复核和关键引用检索反复确认逻辑一致性。

## 五、风险与待确认点

- 当前仓库存在 `.ts` 与编译产物 `.js/.d.ts/.map` 同目录提交的模式，实现时需要避免只改其中一类文件。
- `backend/src/routes/teams.js` 当前已有未提交 6V6 尝试，但 `teams.ts` 未同步；后续会保留思路但以源码重新实现。
- 现有 `AdminView.vue` 体积较大，参数化 5V5/6V6 时需要控制改动范围，避免顺手重构无关的公告/打赏逻辑。
- 段位“未定级”默认分值目前计划保持 `1500`；如你希望未定级按白银下限 `1000` 或其他保守值，需要在实现前确认。
- 玩家看板推荐用一个 `5V5 / 6V6` 切换控件展示两套赛事；如果希望两个模式同时上下展示，也可以改为双面板。
